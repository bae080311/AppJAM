import styled from "styled-components";

const Container = styled.header`
  display: flex;
  justify-content: center;
  align-items: end;
  box-shadow: 0 0 10px 0 rgba(0, 0, 0, 0.1); /* box-shadow로 수정 */
  background-color: rgba(216, 205, 147, 0.19); /* 19% 투명도 */
  padding: 1rem;
  padding-top: 2rem;
`;

export default { Container };
