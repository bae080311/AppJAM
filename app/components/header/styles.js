import styled from "styled-components";

const Container = styled.header`
  display: flex;
  justify-content: center;
  align-items: end;
  bottom-shadow: 0 0 10px 0 rgba(0, 0, 0, 0.1);
  background-color: #d8cd93;
  padding: 1rem;
  padding-top: 2rem;
  opacity: 19%;
`;

export default { Container };
